## To check the working processes: 
lsof -i :5000
kill -9 12345

mlflow ui --backend-store-uri sqlite:///mlflow.db


## Large file, exceeding limit 

echo "Anaconda3-2022.05-Linux-x86_64.sh" >> .gitignore
git add .gitignore
git commit -m "Add .gitignore to exclude large file"

## Add all files except the large one
git add .
git commit -m "Initial commit without large file"

git remote -v
git remote add origin https://github.com/RandaAdel/MLOps.git


